[Back to README](../../../README.md)

# TEX Format (`.tex`)

> **Status:** Not started — stub.

Texture image format used by both IGI 1 and IGI 2. Parsed by the shared `core` TEX reader and
converted to `.tga`. The `.spr` and `.pic` extensions are variants of this format. To be documented.
