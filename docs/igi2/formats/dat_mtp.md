[Back to README](../../../README.md)

# MTP Format (`.mtp`)

> **Status:** Not started — stub.

Material / texture properties data for IGI 2. The parser exists at
`src/igipy/igi2/formats/mtp.py`, but the on-disk format has not yet been documented here.

This page is a placeholder so the `docs/igi2/formats/` layout mirrors the source modules
(`dat_forest`, `dat_graph`, `dat_graphcover`, `dat_mtp`). Fill it in when the `.mtp` structure is
reverse-engineered.
